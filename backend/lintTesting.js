// Missing semicolon
const greeting = 'Hello, World'

// Undeclared variable
name = 'John Doe'

// Using == instead of ===
if (name == 'John Doe') {
    console.log(greeting)
}

// Mixed spaces and tabs, and unexpected space before function parentheses
function sayHello(name) {
    console.log('Hello, ' + name)
}
