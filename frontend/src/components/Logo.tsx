import { Text, Flex } from '@chakra-ui/react';


const Logo = () : JSX.Element => {
    return (
        <Flex className='logo'>
            <Text>
                MentorShip
            </Text>
        </Flex>
    );
};

export default Logo;