{
	"name": "projectone",
	"version": "1.0.0",
	"description": "Bare bones repo for creating a new app",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"format": "npx prettier --write .",
		"lint": "eslint . --ext .ts",
		"prepare": "husky install"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"husky": "^8.0.0"
	}
}
